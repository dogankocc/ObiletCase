@using ObiletService.Core.Application.Features.Queries.BusLocation.List

@{
    Layout = "~/Views/Shared/_layouth.cshtml";
    ViewData["Title"] = "Index";
}
@model GetBusLocationQueryResponse

 <style>
    .navbar-custom {
        height: 50px; 
     
    }

  </style>
<pre>@Model.Data</pre>
<div class="d-flex align-items-center justify-content-center vh-100">

    <div class="container-lg">
        <div class="container-fluid">
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary navbar-custom">

            </nav>
            <div class="list-group rounded-0">
                <div class="dropdown">
                    <a href="#" class="list-group-item list-group-item-action" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1">Nereden</h5>
                        </div>
                        <p class="mb-1">Some placeholder content in a paragraph.</p>
                    </a>
                    <ul class="dropdown-menu">
                        @foreach (var location in Model.Data)
                        {
                            <li><a class="dropdown-item" href="#">@location.Keywords</a></li>

                        }
                    </ul>
                </div>
                <div class="dropdown">
                    <a href="#" class="list-group-item list-group-item-action" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1">Nereye</h5>
                        </div>
                        <p class="mb-1">Some placeholder content in a paragraph.</p>
                    </a>
                    <ul class="dropdown-menu">
                        @foreach (var location in Model.Data)
                        {
                            <li><a class="dropdown-item" href="#">@location.Keywords</a></li>

                        }
                    </ul>
                </div>
          
            </div>
        </div>
  
    </div>
  </div>

     

